name: iv_project_api
description: "IV Project API"
version: 0.0.2
homepage:
publish_to: none

environment:
  sdk: ^3.7.2
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  dio: ^5.8.0+1
  shared_preferences: ^2.5.2

  # iv_project_model:
  #   path: ../iv_project_model

  iv_project_model:
    git:
      url: https://github.com/faequl96/iv_project_model.git
      ref: 0.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
